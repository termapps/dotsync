{
  "private": true,
  "workspaces": {
    "packages": [
      "packages/@dotsync/*"
    ],
    "nohoist": [
      "**/material-icons",
      "**/vuesax"
    ]
  },
  "scripts": {
    "release": "lerna publish --force-publish=*",
    "build:electron": "cd packages/@dotsync/app && yarn build:electron"
  },
  "devDependencies": {
    "lerna": "^3.0.3"
  }
}
