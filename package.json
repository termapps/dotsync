{
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**/material-icons",
      "**/vuesax"
    ]
  },
  "scripts": {
    "release": "lerna publish",
    "release:force": "lerna publish --force-publish",
    "serve": "lerna exec --scope='Dotsync' -- npm run serve:electron",
    "test": "lerna exec --scope='{*,**/core}' -- npm run test",
    "lint": "lerna exec --scope='Dotsync' -- npm run lint"
  },
  "devDependencies": {
    "lerna": "^3.3.0"
  }
}
