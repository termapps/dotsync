{
  "private": true,
  "workspaces": {
    "packages": [
      "packages/@dotsync/*"
    ],
    "nohoist": [
      "**/material-icons",
      "**/vuesax"
    ]
  },
  "scripts": {
    "release": "lerna publish",
    "release:force": "lerna publish --force-publish",
    "test": "lerna exec --scope='Dotsync' -- mocha tests"
  },
  "devDependencies": {
    "lerna": "^3.3.0"
  }
}
